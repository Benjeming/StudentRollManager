<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ludans.studentrollmanager">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestMainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:launchMode="singleTask"/>
        <activity android:name=".Updata"
            android:launchMode="singleTask"/>
        <activity android:name=".Delete"
          android:launchMode="singleTask"/>
        <activity android:name=".Query"
            android:launchMode="singleTask"/>
        <activity android:name=".JiangLi"
            android:launchMode="singleTask"/>
        <activity android:name=".YuanXi"
            android:launchMode="singleTask"/>
        <activity android:name=".ZhuanYe"
            android:launchMode="singleTask"/>

    </application>

</manifest>